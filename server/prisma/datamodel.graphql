type User {
  id: ID! @unique
  name: String!
  phoneNumber: String
  challenges: [Challenge!]! @relation(name: "UserChallenges")
}

type Challenge {
  id: ID! @unique
  title: String!
  users: [User!]! @relation(name: "UserChallenges")
  winner: User @relation(name: "ChallengeWinner")
}
